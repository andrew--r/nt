{
  "name": "gulp-starter",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  "devDependencies": {
    "gulp": "^3.8.8",
    "browser-sync": "^2.0.0",
    "gulp-stylus": "^2.0.0",
    "gulp-autoprefixer": "^1.0.1",
    "gulp-uglify": "^1.1.0",
    "gulp-rename": "^1.2.0"
  }
}
